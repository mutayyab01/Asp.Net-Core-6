@model List<Student>

@{
    ViewData["Title"] = "Contact";
    Layout = null;
}

<h1>Contact</h1>

@{
    foreach (var item in Model)
    {
        <h2>@item.StudID</h2>
        <h2>@item.StudName</h2>
        <h2>@item.Gender</h2>
    }
}