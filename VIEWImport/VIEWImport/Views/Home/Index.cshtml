@model List<Student>
@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

@{
    foreach (var item in Model)
    {
        <h2>@item.StudID</h2>
        <h2>@item.StudName</h2>
        <h2>@item.Gender</h2>
    }
}